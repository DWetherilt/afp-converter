Apply package `2026-02-18-unified-framework-rollup` in `pz-boilerplate-intelliJ`.

Requirements:
1. Merge all included framework/process updates as one coherent change.
2. Ensure policy, build tasks, SQLite state tooling, governance workflows, and boundary-gate enforcement are aligned.
3. Validate with compile + documentation manifest + boundary gate.
4. Commit as a single rollup merge.
