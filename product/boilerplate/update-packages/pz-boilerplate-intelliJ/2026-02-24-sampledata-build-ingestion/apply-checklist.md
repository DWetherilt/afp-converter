# Apply Checklist

1. Create rollback checkpoint in target repo.
2. Apply `afp-cli` build task updates for per-sample conversion.
3. Run build/preview manifest generation.
4. Validate expected outputs in `product/afp-converter/sampleOutput`.
5. Commit and push.
