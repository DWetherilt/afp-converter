# Update Packages (Moved)

Active boilerplate transfer packages now live in the dedicated third realm:
- `product/boilerplate/update-packages/`

Use:
- `product/boilerplate/update-packages/README.md` for current workflow rules and package inventory.
