# Managed Tooling Registration Gate Sync Package

## Purpose
Add constitutional managed-tooling discipline and build enforcement so untracked project-management tooling cannot bypass workflow controls.

## Scope
- First-principle update in policy.
- Build task `enforceManagedTooling`.
- `qualityGate` wiring.
- README/policy docs alignment.
