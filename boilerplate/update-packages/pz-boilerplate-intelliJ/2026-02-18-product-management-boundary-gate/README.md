# Product Management Boundary Gate Sync Package

## Purpose
Add a hard verification gate to prevent product modules from referencing project-management tooling/state.

## Scope
- Add `enforceProjectBoundaries` task.
- Wire into `qualityGate`.
- Update policy/docs to codify mandatory separation.
