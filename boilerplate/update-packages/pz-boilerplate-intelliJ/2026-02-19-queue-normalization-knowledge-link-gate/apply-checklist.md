# Apply Checklist

1. Apply file updates listed in package-manifest.
2. Verify `realmKnowledgeLinkGateStrict` is wired into quality verification.
3. Verify `pmconsole actions --owner <name> --open-only` works.
4. Verify `pmconsole report --request "realm knowledge status"` renders correctly.
5. Run validation commands from package manifest.
